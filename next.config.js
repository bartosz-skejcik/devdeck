/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "www.google.com",
            "s2.googleusercontent.com",
            "res.cloudinary.com",
        ],
    },
};

module.exports = nextConfig;
